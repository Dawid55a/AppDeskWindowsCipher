<Window x:Class="WpfApp.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Views"
        xmlns:wpf="clr-namespace:CipherLibrary.Wpf;assembly=CipherLibrary"
        mc:Ignorable="d"
        Title="PasswordDialog" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <PasswordBox Grid.Row="0" Name="Password"
                     Margin="0,0,0,10"
                     wpf:PasswordBoxAssistant.BindPassword="true"
                     wpf:PasswordBoxAssistant.BoundPassword="{Binding Path=Password, Mode=TwoWay, 
                                                             UpdateSourceTrigger=PropertyChanged}" />
        <Button x:Name="OkButton" Grid.Row="1" Content="OK" IsDefault="True" Command="{Binding SubmitCommand}"/>
    </Grid>
</Window>
